/* jslint node: true, esnext: true */

"use strict";

module.exports = {
	"description": "group of steps",
	"endpoints": {},
	initialize(manager, stepDef) {
		/*
		   for (let sid in stepDef.steps) {
		     const step = stepDef.steps[sid];
		   }*/
	}
};
